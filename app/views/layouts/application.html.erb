<!DOCTYPE html>
<html>
  <head>
    <title>Furima41273</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <% if controller_name == "items" && action_name == "index" %>
    <%= render "shared/header" %>
    <% else %>
    <% end %>

    <%= yield %>

  </body>
</html>